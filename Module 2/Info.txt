Submit :

- `.c` file + `makefile` w/ proper comments
    - header comments
    - all function comments
    - all other comments
- screenshot of build log in terminal (no errors)
- screenshot of AVRDUDE loading code
- link to yt vid of prog on sim vm ?

Steps : 

1. Compile 

  > avr-gcc -mmcu=atmega328p -DF_CPU=16000000UL -Os -I. -DYOURNAME="\"Nathaniel Davis-Perez\"" -c homework1.c -o homework1.o
  > avr-gcc -mmcu=atmega328p homework1.o ./libserial.a -o main.elf

2. Run Simulavr (GUI output) w/ ATMEGA State view window in debugging panel to debug led operation

  > simulavr -d atmega328 -f main.elf -W 0xC6,-

3. Rebuild the code using the 'Release' profile (Ctrl+P , 'task BuildRelease' , 'Never Scan the Task Output for this task)
4. Upload .hex image for relase code to google drive (found in bin/Relase sub-folder)

  > sudo apt install simavr
  > make clean
  > make
  > pkill simavr
  > simavr -m atmega328p -f 16000000 --gdb bin/homework2.elf
    * open vscode *
  > `Ctrl+Shift+D` to open `Run and Debug panel`
  > play `AVR simulavr` *requires adding config to launch.json*
  


  > homework2.c is a AVR program for atmega328p microcontrollers
    that blinks an LED on Port B pin 1. with exactly two functions
    > delay is a busy wait loop that burns CPU cycles coutning through nested loops
    > main initializes hardware and sets bit 1 to 1 then enters infinite
      loop toggling between 0 and 1 each time

  > make file handles the avr build pipeline with tools targets sources
    and flags, compiling and setting up .hex (the file that gets flashed) 
    and .elf (compiled hw2.c)

  > the three watch expressions are the direct memory reads of the
    atmega328p  io register file 
    where x23=PortBinput , x24=PortBdirection , 0x25=PortBoutput

Rubric :

- Proper Toolchain (demonstrate)
The code is written and compiled using the IDE and development toolchain for AVR microcontrollers.

- Stand-alone code (demonstrate)
The program does not include any header files or external libraries.

- Structure and Design Pattern (demonstrate)
The program consists of two functions: delay() and main() and the main function implements a control loop design pattern.

- Hardware: Initialization (demonstrate)
Main function initializes Port B, pin 1 as an output. The direction of all other pins should be left unchanged.

- Hardware: Bit Toggle (demonstrate)
Within the control loop, the value of Port B, pin 1 is toggled. The values of all other pins are left unchanged.

- Delay Function (demonstrate)
The delay function is called from within the control loop to delay approximately 2 seconds each time around the loop.

- Simulation (demonstrate)
Evidence shows the program running properly on the simulator.

- Execution (demonstrate)
Evidence shows the program running properly on the Atmega development board